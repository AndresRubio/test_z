# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Guidance for this repository lives in **[AGENTS.md](AGENTS.md)** — commands,
architecture, and the hard constraints (uv + Python 3.12 only; never write the
client's brand name; `Coding Task.docx` stays gitignored). Read it first.

Supporting docs it points to: `CONTEXT.md` (domain vocabulary), `docs/adr/`
(design decisions), and `README.md` (the graded write-up).
